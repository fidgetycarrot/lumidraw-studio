LumiDraw Studio v0.15.0 build verification

- ZIP must be flat with spindle.json at archive root.
- spindle.json and package.json must both report 0.15.0.
- frontend header must visibly report v0.15.0.
- backend and frontend must pass JavaScript syntax validation.
- list_models must merge Bridge catalog with remembered preset values.
- Bridge model filter must exclude Qwen 3 support weights and tiny LTX stubs.
- batch_count safety remains forced to 1.
